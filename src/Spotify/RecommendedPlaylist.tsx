import { StyleSheet, Text, View } from 'react-native';

export default function RecommendedPlaylist() {
    return (
        <View>
            <Text>RecommendedPlaylist</Text>
        </View>
    );
}

const styles = StyleSheet.create({});
