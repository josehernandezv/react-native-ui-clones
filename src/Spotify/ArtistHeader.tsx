import { StyleSheet, Text, View } from 'react-native';

export default function ArtistHeader() {
    return (
        <View>
            <Text>ArtistHeader</Text>
        </View>
    );
}

const styles = StyleSheet.create({});
